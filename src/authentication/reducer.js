import {

} from './actionTypes'

const initialState = {

}

function authentication(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default authentication
