import {

} from './actionTypes'

const initialState = {

}

function article(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default article
